# Tech-Analysis-Agent — 4개 sibling 에이전트 공용 requirements
# ────────────────────────────────────────────────────────────
#   - 이 파일은 tech_analysis_agent / roadmap_planner_agent /
#     investment_strategist_agent / orchestration_agent 네 폴더의
#     requirements.txt 를 합한 superset 입니다.
#   - 공용 venv 를 루트에 두고 네 에이전트가 모두 이 venv 를 공유하는
#     구조이므로, 아래 목록으로 한 번에 설치하면 됩니다.
#
# 설치:
#   uv venv --python 3.10 .venv && source .venv/bin/activate
#   uv pip install -r requirements.txt
#
# (또는 표준 pip)
#   python3.10 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# ── LangGraph / LangChain 공용 ────────────────────────────────
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-ollama>=0.2.0

# ── 환경변수 / HTTP ──────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0

# ── tech_analysis_agent 전용 (시장/특허 API) ─────────────────
tavily-python>=0.3.0
pydantic>=2.0.0
networkx>=3.0
matplotlib>=3.7.0

# ── 웹 데모 공용 (tech_analysis_agent / orchestration_agent) ─
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# PDF export 는 제거됨 — HTML / Markdown / JSON 만 사용
# 필요 시 브라우저에서 HTML 열고 "PDF 로 인쇄" 사용
